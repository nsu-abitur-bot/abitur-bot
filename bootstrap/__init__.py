"""Подготовка окружения к старту: схема, миграции, справочные данные.

Слой выше `db` и `parser`: заливка проходных баллов тянет страницу итогов приёма
через `parser.scores`, и слою данных это не принадлежит — именно из-за такого
импорта существовал цикл `db <-> parser` (#310).

`db/postgres/init_db.py` остаётся про саму базу: создать, применить миграции.
Что залить после этого — решается здесь.
"""
